{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "npm install -g pnpm && pnpm install && cd apps/dapp-template-evm && rm -rf out .next && pnpm build",
    "outputDirectory": "apps/dapp-template-evm/out",
    "installCommand": "echo 'Skip default install'",
    "framework": null
}