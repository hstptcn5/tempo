{
    "buildCommand": "pnpm turbo build --filter=dapp-template-evm --force && mkdir -p dist && cp -r apps/dapp-template-evm/out/* dist/",
    "outputDirectory": "dist",
    "installCommand": "pnpm install",
    "framework": null
}